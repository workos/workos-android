// This file is auto-generated by oagen. Do not edit.

package com.workos.android

import com.workos.android.enums.AgentAdminValidateCredentialRequestType
import com.workos.android.enums.AgentRegistrationCreatedDataKind
import com.workos.android.enums.AgentRegistrationCreatedDataMethod
import com.workos.android.enums.AgentRegistrationCreatedDataStatus
import com.workos.android.enums.AgentRegistrationKind
import com.workos.android.enums.AgentRegistrationStatus
import com.workos.android.enums.ApplicationsRegistrationTypes
import com.workos.android.enums.AuditLogConfigurationLogStreamState
import com.workos.android.enums.AuditLogConfigurationLogStreamType
import com.workos.android.enums.AuditLogConfigurationState
import com.workos.android.enums.AuditLogExportState
import com.workos.android.enums.AuthenticateResponseAuthenticationMethod
import com.workos.android.enums.AuthenticationFactorEnrolledType
import com.workos.android.enums.AuthenticationFactorType
import com.workos.android.enums.AuthenticationFactorsCreateRequestType
import com.workos.android.enums.AuthenticationRadarRiskDetectedDataAction
import com.workos.android.enums.AuthorizationAssignment
import com.workos.android.enums.ClaimViewResponseStatus
import com.workos.android.enums.ConnectedAccountAuthMethod
import com.workos.android.enums.ConnectedAccountInputState
import com.workos.android.enums.ConnectedAccountState
import com.workos.android.enums.ConnectionActivatedDataConnectionType
import com.workos.android.enums.ConnectionActivatedDataState
import com.workos.android.enums.ConnectionActivatedDataStatus
import com.workos.android.enums.ConnectionDeactivatedDataConnectionType
import com.workos.android.enums.ConnectionDeactivatedDataState
import com.workos.android.enums.ConnectionDeactivatedDataStatus
import com.workos.android.enums.ConnectionDeletedDataConnectionType
import com.workos.android.enums.ConnectionDeletedDataState
import com.workos.android.enums.ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType
import kotlinx.serialization.json.Json
import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue

/**
 * Forward-compatibility coverage for the generated enum serializers: a wire value
 * this SDK does not know must decode into `Unknown` and re-encode unchanged, and a
 * known value must still resolve to its own case.
 */
class EnumForwardCompatTest {
    private val json = Json { ignoreUnknownKeys = true }

    @Test
    fun `AgentAdminValidateCredentialRequestType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentAdminValidateCredentialRequestType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentAdminValidateCredentialRequestType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentAdminValidateCredentialRequestType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentAdminValidateCredentialRequestType.serializer(), "\"api_key\"")
        assertTrue(known !is AgentAdminValidateCredentialRequestType.Unknown)
        assertEquals("api_key", known.rawValue)
    }

    @Test
    fun `AgentRegistrationCreatedDataKind preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentRegistrationCreatedDataKind.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentRegistrationCreatedDataKind.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentRegistrationCreatedDataKind.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentRegistrationCreatedDataKind.serializer(), "\"anonymous\"")
        assertTrue(known !is AgentRegistrationCreatedDataKind.Unknown)
        assertEquals("anonymous", known.rawValue)
    }

    @Test
    fun `AgentRegistrationCreatedDataMethod preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentRegistrationCreatedDataMethod.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentRegistrationCreatedDataMethod.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentRegistrationCreatedDataMethod.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentRegistrationCreatedDataMethod.serializer(), "\"anonymous\"")
        assertTrue(known !is AgentRegistrationCreatedDataMethod.Unknown)
        assertEquals("anonymous", known.rawValue)
    }

    @Test
    fun `AgentRegistrationCreatedDataStatus preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentRegistrationCreatedDataStatus.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentRegistrationCreatedDataStatus.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentRegistrationCreatedDataStatus.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentRegistrationCreatedDataStatus.serializer(), "\"unverified\"")
        assertTrue(known !is AgentRegistrationCreatedDataStatus.Unknown)
        assertEquals("unverified", known.rawValue)
    }

    @Test
    fun `AgentRegistrationKind preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentRegistrationKind.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentRegistrationKind.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentRegistrationKind.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentRegistrationKind.serializer(), "\"anonymous\"")
        assertTrue(known !is AgentRegistrationKind.Unknown)
        assertEquals("anonymous", known.rawValue)
    }

    @Test
    fun `AgentRegistrationStatus preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AgentRegistrationStatus.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AgentRegistrationStatus.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AgentRegistrationStatus.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AgentRegistrationStatus.serializer(), "\"unverified\"")
        assertTrue(known !is AgentRegistrationStatus.Unknown)
        assertEquals("unverified", known.rawValue)
    }

    @Test
    fun `ApplicationsRegistrationTypes preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ApplicationsRegistrationTypes.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ApplicationsRegistrationTypes.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ApplicationsRegistrationTypes.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ApplicationsRegistrationTypes.serializer(), "\"dynamic\"")
        assertTrue(known !is ApplicationsRegistrationTypes.Unknown)
        assertEquals("dynamic", known.rawValue)
    }

    @Test
    fun `AuditLogConfigurationLogStreamState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuditLogConfigurationLogStreamState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuditLogConfigurationLogStreamState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuditLogConfigurationLogStreamState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuditLogConfigurationLogStreamState.serializer(), "\"active\"")
        assertTrue(known !is AuditLogConfigurationLogStreamState.Unknown)
        assertEquals("active", known.rawValue)
    }

    @Test
    fun `AuditLogConfigurationLogStreamType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuditLogConfigurationLogStreamType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuditLogConfigurationLogStreamType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuditLogConfigurationLogStreamType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuditLogConfigurationLogStreamType.serializer(), "\"AzureSentinel\"")
        assertTrue(known !is AuditLogConfigurationLogStreamType.Unknown)
        assertEquals("AzureSentinel", known.rawValue)
    }

    @Test
    fun `AuditLogConfigurationState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuditLogConfigurationState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuditLogConfigurationState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuditLogConfigurationState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuditLogConfigurationState.serializer(), "\"active\"")
        assertTrue(known !is AuditLogConfigurationState.Unknown)
        assertEquals("active", known.rawValue)
    }

    @Test
    fun `AuditLogExportState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuditLogExportState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuditLogExportState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuditLogExportState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuditLogExportState.serializer(), "\"pending\"")
        assertTrue(known !is AuditLogExportState.Unknown)
        assertEquals("pending", known.rawValue)
    }

    @Test
    fun `AuthenticateResponseAuthenticationMethod preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthenticateResponseAuthenticationMethod.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthenticateResponseAuthenticationMethod.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthenticateResponseAuthenticationMethod.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthenticateResponseAuthenticationMethod.serializer(), "\"SSO\"")
        assertTrue(known !is AuthenticateResponseAuthenticationMethod.Unknown)
        assertEquals("SSO", known.rawValue)
    }

    @Test
    fun `AuthenticationFactorEnrolledType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthenticationFactorEnrolledType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthenticationFactorEnrolledType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthenticationFactorEnrolledType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthenticationFactorEnrolledType.serializer(), "\"generic_otp\"")
        assertTrue(known !is AuthenticationFactorEnrolledType.Unknown)
        assertEquals("generic_otp", known.rawValue)
    }

    @Test
    fun `AuthenticationFactorsCreateRequestType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthenticationFactorsCreateRequestType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthenticationFactorsCreateRequestType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthenticationFactorsCreateRequestType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthenticationFactorsCreateRequestType.serializer(), "\"generic_otp\"")
        assertTrue(known !is AuthenticationFactorsCreateRequestType.Unknown)
        assertEquals("generic_otp", known.rawValue)
    }

    @Test
    fun `AuthenticationFactorType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthenticationFactorType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthenticationFactorType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthenticationFactorType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthenticationFactorType.serializer(), "\"generic_otp\"")
        assertTrue(known !is AuthenticationFactorType.Unknown)
        assertEquals("generic_otp", known.rawValue)
    }

    @Test
    fun `AuthenticationRadarRiskDetectedDataAction preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthenticationRadarRiskDetectedDataAction.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthenticationRadarRiskDetectedDataAction.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthenticationRadarRiskDetectedDataAction.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthenticationRadarRiskDetectedDataAction.serializer(), "\"signup\"")
        assertTrue(known !is AuthenticationRadarRiskDetectedDataAction.Unknown)
        assertEquals("signup", known.rawValue)
    }

    @Test
    fun `AuthorizationAssignment preserves an unknown wire value`() {
        val decoded = json.decodeFromString(AuthorizationAssignment.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(AuthorizationAssignment.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(AuthorizationAssignment.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(AuthorizationAssignment.serializer(), "\"direct\"")
        assertTrue(known !is AuthorizationAssignment.Unknown)
        assertEquals("direct", known.rawValue)
    }

    @Test
    fun `ClaimViewResponseStatus preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ClaimViewResponseStatus.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ClaimViewResponseStatus.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ClaimViewResponseStatus.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ClaimViewResponseStatus.serializer(), "\"unverified\"")
        assertTrue(known !is ClaimViewResponseStatus.Unknown)
        assertEquals("unverified", known.rawValue)
    }

    @Test
    fun `ConnectedAccountAuthMethod preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectedAccountAuthMethod.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectedAccountAuthMethod.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectedAccountAuthMethod.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectedAccountAuthMethod.serializer(), "\"oauth\"")
        assertTrue(known !is ConnectedAccountAuthMethod.Unknown)
        assertEquals("oauth", known.rawValue)
    }

    @Test
    fun `ConnectedAccountInputState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectedAccountInputState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectedAccountInputState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectedAccountInputState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectedAccountInputState.serializer(), "\"connected\"")
        assertTrue(known !is ConnectedAccountInputState.Unknown)
        assertEquals("connected", known.rawValue)
    }

    @Test
    fun `ConnectedAccountState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectedAccountState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectedAccountState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectedAccountState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectedAccountState.serializer(), "\"connected\"")
        assertTrue(known !is ConnectedAccountState.Unknown)
        assertEquals("connected", known.rawValue)
    }

    @Test
    fun `ConnectionActivatedDataConnectionType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionActivatedDataConnectionType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionActivatedDataConnectionType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionActivatedDataConnectionType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionActivatedDataConnectionType.serializer(), "\"ADFSSAML\"")
        assertTrue(known !is ConnectionActivatedDataConnectionType.Unknown)
        assertEquals("ADFSSAML", known.rawValue)
    }

    @Test
    fun `ConnectionActivatedDataState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionActivatedDataState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionActivatedDataState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionActivatedDataState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionActivatedDataState.serializer(), "\"draft\"")
        assertTrue(known !is ConnectionActivatedDataState.Unknown)
        assertEquals("draft", known.rawValue)
    }

    @Test
    fun `ConnectionActivatedDataStatus preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionActivatedDataStatus.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionActivatedDataStatus.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionActivatedDataStatus.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionActivatedDataStatus.serializer(), "\"linked\"")
        assertTrue(known !is ConnectionActivatedDataStatus.Unknown)
        assertEquals("linked", known.rawValue)
    }

    @Test
    fun `ConnectionDeactivatedDataConnectionType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionDeactivatedDataConnectionType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionDeactivatedDataConnectionType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionDeactivatedDataConnectionType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionDeactivatedDataConnectionType.serializer(), "\"ADFSSAML\"")
        assertTrue(known !is ConnectionDeactivatedDataConnectionType.Unknown)
        assertEquals("ADFSSAML", known.rawValue)
    }

    @Test
    fun `ConnectionDeactivatedDataState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionDeactivatedDataState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionDeactivatedDataState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionDeactivatedDataState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionDeactivatedDataState.serializer(), "\"draft\"")
        assertTrue(known !is ConnectionDeactivatedDataState.Unknown)
        assertEquals("draft", known.rawValue)
    }

    @Test
    fun `ConnectionDeactivatedDataStatus preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionDeactivatedDataStatus.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionDeactivatedDataStatus.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionDeactivatedDataStatus.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionDeactivatedDataStatus.serializer(), "\"linked\"")
        assertTrue(known !is ConnectionDeactivatedDataStatus.Unknown)
        assertEquals("linked", known.rawValue)
    }

    @Test
    fun `ConnectionDeletedDataConnectionType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionDeletedDataConnectionType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionDeletedDataConnectionType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionDeletedDataConnectionType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionDeletedDataConnectionType.serializer(), "\"ADFSSAML\"")
        assertTrue(known !is ConnectionDeletedDataConnectionType.Unknown)
        assertEquals("ADFSSAML", known.rawValue)
    }

    @Test
    fun `ConnectionDeletedDataState preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionDeletedDataState.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionDeletedDataState.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionDeletedDataState.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionDeletedDataState.serializer(), "\"draft\"")
        assertTrue(known !is ConnectionDeletedDataState.Unknown)
        assertEquals("draft", known.rawValue)
    }

    @Test
    fun `ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType preserves an unknown wire value`() {
        val decoded = json.decodeFromString(ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType.serializer(), "\"totally-new-value-from-the-api\"")

        assertEquals(ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType.Unknown("totally-new-value-from-the-api"), decoded)
        assertEquals("\"totally-new-value-from-the-api\"", json.encodeToString(ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType.serializer(), decoded))

        // A known value must still resolve to its own case, not Unknown.
        val known = json.decodeFromString(ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType.serializer(), "\"ResponseSigning\"")
        assertTrue(known !is ConnectionSAMLCertificateRenewalRequiredDataCertificateCertificateType.Unknown)
        assertEquals("ResponseSigning", known.rawValue)
    }
}
