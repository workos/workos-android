// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.AuditLogConfiguration
import com.workos.android.models.Organization
import com.workos.android.models.OrganizationAuthorizedConnectApplicationListData
import com.workos.android.models.OrganizationDomainData
import kotlinx.coroutines.flow.Flow

/** Operations for the Organizations API. */
public class Organizations internal constructor(
    private val transport: Transport,
) {
    /**
     * List Organizations
     *
     * Get a list of all of your existing organizations matching the criteria specified.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param domains The domains of an Organization. Any Organization with a matching domain will be returned.
     * @param search Searchable text for an Organization. Matches against the organization name.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<Organization>` returned by the API.
     */
    public suspend fun list(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        domains: List<String>? = null,
        search: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Organization> {
        val path = "organizations"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        domains?.let {
            for (value in it) {
                query.add(QueryParam("domains", value))
            }
        }
        search?.let { query.add(QueryParam("search", it)) }
        return transport.request<Page<Organization>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [list]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param domains The domains of an Organization. Any Organization with a matching domain will be returned.
     * @param search Searchable text for an Organization. Matches against the organization name.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `Organization` values.
     */
    public fun listAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        domains: List<String>? = null,
        search: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<Organization> =
        autoPagingFlow { cursor ->
            list(
                before = if (cursor == null) before else null,
                after = cursor,
                limit = limit,
                order = order,
                domains = domains,
                search = search,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create an Organization
     *
     * Creates a new organization in the current environment.
     *
     * @param name The name of the organization.
     * @param allowProfilesOutsideOrganization Whether the organization allows profiles from outside the organization to sign in.
     * @param domains The domains associated with the organization. Deprecated in favor of `domain_data`.
     * @param domainData The domains associated with the organization, including verification state.
     * @param metadata Object containing [metadata](https://workos.com/docs/authkit/metadata) key/value pairs associated with the Organization.
     * @param externalId An external identifier for the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Organization` returned by the API.
     */
    public suspend fun create(
        name: String,
        allowProfilesOutsideOrganization: Boolean? = null,
        domains: List<String>? = null,
        domainData: List<OrganizationDomainData>? = null,
        metadata: Map<String, String>? = null,
        externalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Organization {
        val path = "organizations"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("allow_profiles_outside_organization", allowProfilesOutsideOrganization)
        payload.set("domains", domains)
        payload.set("domain_data", domainData)
        payload.set("metadata", metadata)
        payload.set("external_id", externalId)
        return transport.request<Organization>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get an Organization by External ID
     *
     * Get the details of an existing organization by an [external identifier](https://workos.com/docs/authkit/metadata/external-identifiers).
     *
     * @param externalId The external ID of the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Organization` returned by the API.
     */
    public suspend fun getByExternalId(
        externalId: String,
        requestOptions: RequestOptions? = null,
    ): Organization {
        val path = "organizations/external_id/${PathEncoding.segment(externalId)}"
        return transport.request<Organization>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Get an Organization
     *
     * Get the details of an existing organization.
     *
     * @param id Unique identifier of the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Organization` returned by the API.
     */
    public suspend fun get(
        id: String,
        requestOptions: RequestOptions? = null,
    ): Organization {
        val path = "organizations/${PathEncoding.segment(id)}"
        return transport.request<Organization>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update an Organization
     *
     * Updates an organization in the current environment.
     *
     * @param id Unique identifier of the Organization.
     * @param name The name of the organization.
     * @param allowProfilesOutsideOrganization Whether the organization allows profiles from outside the organization to sign in.
     * @param domains Deprecated. The domains associated with the organization. Deprecated in favor of `domain_data`.
     * @param domainData The domains associated with the organization, including verification state.
     * @param stripeCustomerId The Stripe customer ID associated with the organization.
     * @param metadata Object containing [metadata](https://workos.com/docs/authkit/metadata) key/value pairs associated with the Organization.
     * @param externalId An external identifier for the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Organization` returned by the API.
     */
    public suspend fun update(
        id: String,
        name: String? = null,
        allowProfilesOutsideOrganization: Boolean? = null,
        domains: List<String>? = null,
        domainData: List<OrganizationDomainData>? = null,
        stripeCustomerId: String? = null,
        metadata: Map<String, String>? = null,
        externalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Organization {
        val path = "organizations/${PathEncoding.segment(id)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("allow_profiles_outside_organization", allowProfilesOutsideOrganization)
        payload.set("domains", domains)
        payload.set("domain_data", domainData)
        payload.set("stripe_customer_id", stripeCustomerId)
        payload.set("metadata", metadata)
        payload.set("external_id", externalId)
        return transport.request<Organization>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete an Organization
     *
     * Permanently deletes an organization in the current environment. It cannot be undone.
     *
     * @param id Unique identifier of the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun delete(
        id: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "organizations/${PathEncoding.segment(id)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Get Audit Log Configuration
     *
     * Get the unified view of audit log trail and stream configuration for an organization.
     *
     * @param id Unique identifier of the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogConfiguration` returned by the API.
     */
    public suspend fun getAuditLogConfiguration(
        id: String,
        requestOptions: RequestOptions? = null,
    ): AuditLogConfiguration {
        val path = "organizations/${PathEncoding.segment(id)}/audit_log_configuration"
        return transport.request<AuditLogConfiguration>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List authorized applications
     *
     * Get a list of all Connect applications that users in the organization have authorized.
     *
     * @param organizationId The ID of the organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<OrganizationAuthorizedConnectApplicationListData>` returned by the API.
     */
    public suspend fun listAuthorizedApplications(
        organizationId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<OrganizationAuthorizedConnectApplicationListData> {
        val path = "organizations/${PathEncoding.segment(organizationId)}/authorized_applications"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<OrganizationAuthorizedConnectApplicationListData>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listAuthorizedApplications]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId The ID of the organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `OrganizationAuthorizedConnectApplicationListData` values.
     */
    public fun listAuthorizedApplicationsAutoPaging(
        organizationId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<OrganizationAuthorizedConnectApplicationListData> =
        autoPagingFlow { cursor ->
            listAuthorizedApplications(
                organizationId = organizationId,
                before = if (cursor == null) before else null,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }
}
