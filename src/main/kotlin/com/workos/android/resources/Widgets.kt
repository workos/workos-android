// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.RequestOptions
import com.workos.android.enums.WidgetSessionTokenScopes
import com.workos.android.internal.JsonBody
import com.workos.android.internal.Transport
import com.workos.android.models.WidgetSessionTokenResponse

/** Operations for the Widgets API. */
public class Widgets internal constructor(
    private val transport: Transport,
) {
    /**
     * Generate a widget token
     *
     * Generate a widget token for a user, optionally scoped to an organization. When an organization is specified, org-scoped widgets are enabled; omitting it issues a user-only token for widgets like `UserProfile` and `UserSecurity`.
     *
     * @param organizationId The ID of the organization to scope the widget session to. Required when scopes are provided. Optional when issuing a token for user-only widgets (e.g. `UserProfile`, `UserSecurity`) that do not require organization context.
     * @param userId The ID of the user to issue the widget session token for.
     * @param scopes The scopes to grant the widget session.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `WidgetSessionTokenResponse` returned by the API.
     */
    public suspend fun createToken(
        organizationId: String? = null,
        userId: String? = null,
        scopes: List<WidgetSessionTokenScopes>? = null,
        requestOptions: RequestOptions? = null,
    ): WidgetSessionTokenResponse {
        val path = "widgets/token"
        val payload = JsonBody()
        payload.set("organization_id", organizationId)
        payload.set("user_id", userId)
        payload.set("scopes", scopes)
        return transport.request<WidgetSessionTokenResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }
}
