// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.ConnectionsConnectionType
import com.workos.android.enums.PaginationOrder
import com.workos.android.enums.SSOProvider
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.Connection
import com.workos.android.models.Profile
import com.workos.android.models.SSOLogoutAuthorizeResponse
import com.workos.android.models.SSOTokenResponse
import kotlinx.coroutines.flow.Flow

/** Operations for the SSO API. */
public class SSO internal constructor(
    private val transport: Transport,
) {
    /**
     * List Connections
     *
     * Get a list of all of your existing connections matching the criteria specified.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param connectionType Filter Connections by their type.
     * @param domain Filter Connections by their associated domain.
     * @param organizationId Filter Connections by their associated organization.
     * @param search Searchable text to match against Connection names.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<Connection>` returned by the API.
     */
    public suspend fun listConnections(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        connectionType: ConnectionsConnectionType? = null,
        domain: String? = null,
        organizationId: String? = null,
        search: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Connection> {
        val path = "connections"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        connectionType?.let { query.add(QueryParam("connection_type", it.rawValue)) }
        domain?.let { query.add(QueryParam("domain", it)) }
        organizationId?.let { query.add(QueryParam("organization_id", it)) }
        search?.let { query.add(QueryParam("search", it)) }
        return transport.request<Page<Connection>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listConnections]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param connectionType Filter Connections by their type.
     * @param domain Filter Connections by their associated domain.
     * @param organizationId Filter Connections by their associated organization.
     * @param search Searchable text to match against Connection names.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `Connection` values.
     */
    public fun listConnectionsAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        connectionType: ConnectionsConnectionType? = null,
        domain: String? = null,
        organizationId: String? = null,
        search: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<Connection> =
        autoPagingFlow { cursor ->
            listConnections(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                connectionType = connectionType,
                domain = domain,
                organizationId = organizationId,
                search = search,
                requestOptions = requestOptions,
            )
        }

    /**
     * Get a Connection
     *
     * Get the details of an existing connection.
     *
     * @param id Unique identifier for the Connection.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Connection` returned by the API.
     */
    public suspend fun getConnection(
        id: String,
        requestOptions: RequestOptions? = null,
    ): Connection {
        val path = "connections/${PathEncoding.segment(id)}"
        return transport.request<Connection>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Delete a Connection
     *
     * Permanently deletes an existing connection. It cannot be undone.
     *
     * @param id Unique identifier for the Connection.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteConnection(
        id: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "connections/${PathEncoding.segment(id)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Initiate SSO
     *
     * Initiates the single sign-on flow.
     *
     * @param redirectUri Where to redirect the user after they complete the authentication process. You must use one of the redirect URIs configured via the [Redirects](https://dashboard.workos.com/redirects) page on the dashboard.
     * @param providerScopes Additional scopes to request from the identity provider. Applicable when using OAuth or OpenID Connect connections.
     * @param providerQueryParams Key/value pairs of query parameters to pass to the OAuth provider. Only applicable when using OAuth connections.
     * @param domain Deprecated. Use `connection` or `organization` instead. Used to initiate SSO for a connection by domain. The domain must be associated with a connection in your WorkOS environment.
     * @param provider Used to initiate OAuth authentication with various providers.
     * @param state An optional parameter that can be used to encode arbitrary information to help restore application state between redirects. If included, the redirect URI received from WorkOS will contain the exact `state` that was passed.
     * @param connection Used to initiate SSO for a connection. The value should be a WorkOS connection ID.
     * @param organization Used to initiate SSO for an organization. The value should be a WorkOS organization ID.
     * @param domainHint Can be used to pre-fill the domain field when initiating authentication with Microsoft OAuth or with a Google SAML connection type.
     * @param loginHint Can be used to pre-fill the username/email address field of the IdP sign-in page for the user, if you know their username ahead of time. Currently supported for OAuth, OpenID Connect, Okta, Entra ID, and custom SAML connections.
     * @param nonce A random string generated by the client that is used to mitigate replay attacks.
     * @param prompt If set to `login`, forces re-authentication at the identity provider. For supported SAML providers this sets `ForceAuthn="true"` in the SAML request; providers that don't support it are unaffected.
     * @return The assembled URL to redirect the user to.
     */
    public fun getAuthorizationUrl(
        redirectUri: String,
        providerScopes: List<String>? = null,
        providerQueryParams: Map<String, String>? = null,
        domain: String? = null,
        provider: SSOProvider? = null,
        state: String? = null,
        connection: String? = null,
        organization: String? = null,
        domainHint: String? = null,
        loginHint: String? = null,
        nonce: String? = null,
        prompt: String? = null,
    ): String {
        val path = "sso/authorize"
        val query = mutableListOf<QueryParam>()
        query.add(QueryParam("response_type", "code"))
        transport.configuration.clientId?.let { query.add(QueryParam("client_id", it)) }
        providerScopes?.let {
            for (value in it) {
                query.add(QueryParam("provider_scopes", value))
            }
        }
        providerQueryParams?.let { query.add(QueryParam("provider_query_params", it.toString())) }
        domain?.let { query.add(QueryParam("domain", it)) }
        provider?.let { query.add(QueryParam("provider", it.rawValue)) }
        query.add(QueryParam("redirect_uri", redirectUri))
        state?.let { query.add(QueryParam("state", it)) }
        connection?.let { query.add(QueryParam("connection", it)) }
        organization?.let { query.add(QueryParam("organization", it)) }
        domainHint?.let { query.add(QueryParam("domain_hint", it)) }
        loginHint?.let { query.add(QueryParam("login_hint", it)) }
        nonce?.let { query.add(QueryParam("nonce", it)) }
        prompt?.let { query.add(QueryParam("prompt", it.toString())) }
        return transport.buildUrl(path, query)
    }

    /**
     * Logout Redirect
     *
     * Logout allows to sign out a user from your application by triggering the identity provider sign out flow. This `GET` endpoint should be a redirection, since the identity provider user will be identified in the browser session.
     *
     * Before redirecting to this endpoint, you need to generate a short-lived logout token using the [Logout Authorize](https://workos.com/docs/reference/sso/logout/authorize) endpoint.
     *
     * @param token The logout token returned from the [Logout Authorize](https://workos.com/docs/reference/sso/logout/authorize) endpoint.
     * @return The assembled URL to redirect the user to.
     */
    public fun getLogoutUrl(token: String): String {
        val path = "sso/logout"
        val query = mutableListOf<QueryParam>()
        query.add(QueryParam("token", token))
        return transport.buildUrl(path, query)
    }

    /**
     * Logout Authorize
     *
     * You should call this endpoint from your server to generate a logout token which is required for the [Logout Redirect](https://workos.com/docs/reference/sso/logout) endpoint.
     *
     * @param profileId The unique ID of the profile to log out.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `SSOLogoutAuthorizeResponse` returned by the API.
     */
    public suspend fun authorizeLogout(
        profileId: String,
        requestOptions: RequestOptions? = null,
    ): SSOLogoutAuthorizeResponse {
        val path = "sso/logout/authorize"
        val payload = JsonBody()
        payload.set("profile_id", profileId)
        return transport.request<SSOLogoutAuthorizeResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a User Profile
     *
     * Exchange an access token for a user's [Profile](https://workos.com/docs/reference/sso/profile). Because this profile is returned in the [Get a Profile and Token endpoint](https://workos.com/docs/reference/sso/profile/get-profile-and-token) your application usually does not need to call this endpoint. It is available for any authentication flows that require an additional endpoint to retrieve a user's profile.
     *
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Profile` returned by the API.
     */
    public suspend fun getProfile(requestOptions: RequestOptions? = null): Profile {
        val path = "sso/profile"
        return transport.request<Profile>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Get a Profile and Token
     *
     * Get an access token along with the user [Profile](https://workos.com/docs/reference/sso/profile) using the code passed to your [Redirect URI](https://workos.com/docs/reference/sso/get-authorization-url/redirect-uri).
     *
     * @param code The authorization code received from the authorization callback.
     * @param code2 The authorization code received from the authorization callback.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `SSOTokenResponse` returned by the API.
     */
    public suspend fun getProfileAndToken(
        code: String,
        code2: String,
        requestOptions: RequestOptions? = null,
    ): SSOTokenResponse {
        val path = "sso/token"
        val query = mutableListOf<QueryParam>()
        query.add(QueryParam("code", code2))
        val payload = JsonBody()
        payload.set("code", code)
        payload.set("grant_type", "authorization_code")
        payload.set("client_id", transport.configuration.clientId)
        payload.set("client_secret", transport.configuration.apiKey)
        return transport.request<SSOTokenResponse>(
            method = "POST",
            path = path,
            query = query,
            body = payload,
            options = requestOptions,
        )
    }
}
