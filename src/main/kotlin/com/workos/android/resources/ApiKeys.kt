// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.ApiKey
import com.workos.android.models.ApiKeyValidationResponse
import com.workos.android.models.OrganizationApiKey
import com.workos.android.models.OrganizationApiKeyWithValue
import kotlinx.coroutines.flow.Flow
import kotlinx.datetime.Instant

/** Operations for the ApiKeys API. */
public class ApiKeys internal constructor(
    private val transport: Transport,
) {
    /**
     * List API keys for an organization
     *
     * Get a list of all API keys for an organization.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Page<OrganizationApiKey>` returned by the API.
     */
    public suspend fun listOrganizationApiKeys(
        organizationId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<OrganizationApiKey> {
        val path = "organizations/${PathEncoding.segment(organizationId)}/api_keys"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<OrganizationApiKey>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listOrganizationApiKeys]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return A cold flow of `OrganizationApiKey` values.
     */
    public fun listOrganizationApiKeysAutoPaging(
        organizationId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<OrganizationApiKey> =
        autoPagingFlow { cursor ->
            listOrganizationApiKeys(
                organizationId = organizationId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create an API key for an organization
     *
     * Create a new API key for an organization.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param name The name for the API key.
     * @param permissions The permission slugs to assign to the API key.
     * @param expiresAt The timestamp when the API key should expire. Must be a future timestamp. If omitted, the key does not expire.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `OrganizationApiKeyWithValue` returned by the API.
     */
    public suspend fun createOrganizationApiKey(
        organizationId: String,
        name: String,
        permissions: List<String>? = null,
        expiresAt: Instant? = null,
        requestOptions: RequestOptions? = null,
    ): OrganizationApiKeyWithValue {
        val path = "organizations/${PathEncoding.segment(organizationId)}/api_keys"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("permissions", permissions)
        payload.set("expires_at", expiresAt)
        return transport.request<OrganizationApiKeyWithValue>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Validate API key
     *
     * Validate an API key value and return the API key object if valid.
     *
     * @param value The value for an API key.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `ApiKeyValidationResponse` returned by the API.
     */
    public suspend fun createValidation(
        value: String,
        requestOptions: RequestOptions? = null,
    ): ApiKeyValidationResponse {
        val path = "api_keys/validations"
        val payload = JsonBody()
        payload.set("value", value)
        return transport.request<ApiKeyValidationResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete an API key
     *
     * Permanently deletes an API key. This action cannot be undone. Once deleted, any requests using this API key will fail authentication.
     *
     * @param id The unique ID of the API key.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     */
    public suspend fun delete(
        id: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "api_keys/${PathEncoding.segment(id)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Expire an API key
     *
     * Expire an API key immediately, schedule a future expiration, or clear a scheduled future expiration.
     *
     * @param id The unique ID of the API key.
     * @param expiresAt When the API key should expire. If omitted or in the past, the key expires immediately. Use null to clear a scheduled future expiration.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `ApiKey` returned by the API.
     */
    public suspend fun createExpire(
        id: String,
        expiresAt: Instant? = null,
        requestOptions: RequestOptions? = null,
    ): ApiKey {
        val path = "api_keys/${PathEncoding.segment(id)}/expire"
        val payload = JsonBody()
        payload.set("expires_at", expiresAt)
        return transport.request<ApiKey>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }
}
