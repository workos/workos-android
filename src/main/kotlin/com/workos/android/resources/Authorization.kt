// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.AuthorizationAssignment
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.AuthorizationCheck
import com.workos.android.models.AuthorizationPermission
import com.workos.android.models.AuthorizationResource
import com.workos.android.models.GroupRoleAssignment
import com.workos.android.models.GroupRoleAssignmentList
import com.workos.android.models.Permission
import com.workos.android.models.ReplaceGroupRoleAssignmentEntry
import com.workos.android.models.Role
import com.workos.android.models.RoleList
import com.workos.android.models.UserOrganizationMembershipBaseListData
import com.workos.android.models.UserRoleAssignment
import kotlinx.coroutines.flow.Flow

/** Operations for the Authorization API. */
public class Authorization internal constructor(
    private val transport: Transport,
) {
    /**
     * List role assignments for a group
     *
     * List all role assignments granted to a group. Each assignment represents a role granted to the group on a resource.
     *
     * @param groupId The ID of the group.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<GroupRoleAssignment>` returned by the API.
     */
    public suspend fun listGroupRoleAssignments(
        groupId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<GroupRoleAssignment> {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<GroupRoleAssignment>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listGroupRoleAssignments]: emits every item across successive pages as the flow is collected.
     *
     * @param groupId The ID of the group.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `GroupRoleAssignment` values.
     */
    public fun listGroupRoleAssignmentsAutoPaging(
        groupId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<GroupRoleAssignment> =
        autoPagingFlow { cursor ->
            listGroupRoleAssignments(
                groupId = groupId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Assign a role to a group
     *
     * Assign a role to a group on a specific resource.
     *
     * @param groupId The ID of the group.
     * @param roleSlug The slug of the role to assign to the group.
     * @param resourceId The ID of the resource. Omit along with the external-id fields to target the organization itself.
     * @param resourceExternalId The external ID of the resource.
     * @param resourceTypeSlug The resource type slug.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `GroupRoleAssignment` returned by the API.
     */
    public suspend fun createGroupRoleAssignment(
        groupId: String,
        roleSlug: String,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): GroupRoleAssignment {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments"
        val payload = JsonBody()
        payload.set("role_slug", roleSlug)
        payload.set("resource_id", resourceId)
        payload.set("resource_external_id", resourceExternalId)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<GroupRoleAssignment>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Replace all role assignments for a group
     *
     * Replace all role assignments for a group with the provided list. Existing assignments not in the list will be removed.
     *
     * @param groupId The ID of the group.
     * @param roleAssignments The list of role assignments that should exist for the group. All existing assignments will be replaced.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `GroupRoleAssignmentList` returned by the API.
     */
    public suspend fun updateGroupRoleAssignments(
        groupId: String,
        roleAssignments: List<ReplaceGroupRoleAssignmentEntry>,
        requestOptions: RequestOptions? = null,
    ): GroupRoleAssignmentList {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments"
        val payload = JsonBody()
        payload.set("role_assignments", roleAssignments)
        return transport.request<GroupRoleAssignmentList>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove group role assignments by criteria
     *
     * Remove role assignments from a group that match the provided criteria. Returns 404 when no matching active assignment is found.
     *
     * @param groupId The ID of the group.
     * @param roleSlug The slug of the role to remove assignments for.
     * @param resourceId The ID of the resource. Mutually exclusive with `resource_external_id` and `resource_type_slug`.
     * @param resourceExternalId The external ID of the resource.
     * @param resourceTypeSlug The resource type slug.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteGroupRoleAssignments(
        groupId: String,
        roleSlug: String,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments"
        val payload = JsonBody()
        payload.set("role_slug", roleSlug)
        payload.set("resource_id", resourceId)
        payload.set("resource_external_id", resourceExternalId)
        payload.set("resource_type_slug", resourceTypeSlug)
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a group role assignment
     *
     * Get a specific role assignment for a group by its ID.
     *
     * @param groupId The ID of the group.
     * @param roleAssignmentId The ID of the group role assignment.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `GroupRoleAssignment` returned by the API.
     */
    public suspend fun getGroupRoleAssignment(
        groupId: String,
        roleAssignmentId: String,
        requestOptions: RequestOptions? = null,
    ): GroupRoleAssignment {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments/${PathEncoding.segment(roleAssignmentId)}"
        return transport.request<GroupRoleAssignment>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Remove a group role assignment
     *
     * Remove a specific role assignment from a group by its ID.
     *
     * @param groupId The ID of the group.
     * @param roleAssignmentId The ID of the group role assignment to remove.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteGroupRoleAssignment(
        groupId: String,
        roleAssignmentId: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/groups/${PathEncoding.segment(groupId)}/role_assignments/${PathEncoding.segment(roleAssignmentId)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Check authorization
     *
     * Check if an organization membership has a specific permission on a resource. Supports identification by resource_id OR by resource_external_id + resource_type_slug.
     *
     * @param organizationMembershipId The ID of the organization membership to check.
     * @param permissionSlug The slug of the permission to check.
     * @param resourceId The ID of the resource. Mutually exclusive with `resource_external_id` and `resource_type_slug`.
     * @param resourceExternalId The external ID of the resource. Required with `resource_type_slug`. Mutually exclusive with `resource_id`.
     * @param resourceTypeSlug The slug of the resource type. Required with `resource_external_id`. Mutually exclusive with `resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationCheck` returned by the API.
     */
    public suspend fun check(
        organizationMembershipId: String,
        permissionSlug: String,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): AuthorizationCheck {
        val path = "authorization/organization_memberships/${PathEncoding.segment(organizationMembershipId)}/check"
        val payload = JsonBody()
        payload.set("permission_slug", permissionSlug)
        payload.set("resource_id", resourceId)
        payload.set("resource_external_id", resourceExternalId)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<AuthorizationCheck>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * List resources for organization membership
     *
     * Returns all child resources of a parent resource where the organization membership has a specific permission. This is useful for resource discovery—answering "What projects can this user access in this workspace?"
     *
     * You must provide either `parent_resource_id` or both `parent_resource_external_id` and `parent_resource_type_slug` to identify the parent resource.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param permissionSlug The permission slug to filter by. Only child resources where the organization membership has this permission are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param parentResourceId The WorkOS ID of the parent resource. Provide this or both `parent_resource_external_id` and `parent_resource_type_slug`, but not both. Mutually exclusive with `parent_resource_type_slug` and `parent_resource_external_id`.
     * @param parentResourceTypeSlug The slug of the parent resource type. Must be provided together with `parent_resource_external_id`. Required with `parent_resource_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param parentResourceExternalId The application-specific external identifier of the parent resource. Must be provided together with `parent_resource_type_slug`. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuthorizationResource>` returned by the API.
     */
    public suspend fun listResourcesForMembership(
        organizationMembershipId: String,
        permissionSlug: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        parentResourceId: String? = null,
        parentResourceTypeSlug: String? = null,
        parentResourceExternalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuthorizationResource> {
        val path = "authorization/organization_memberships/${PathEncoding.segment(organizationMembershipId)}/resources"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        query.add(QueryParam("permission_slug", permissionSlug))
        parentResourceId?.let { query.add(QueryParam("parent_resource_id", it)) }
        parentResourceTypeSlug?.let { query.add(QueryParam("parent_resource_type_slug", it)) }
        parentResourceExternalId?.let { query.add(QueryParam("parent_resource_external_id", it)) }
        return transport.request<Page<AuthorizationResource>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listResourcesForMembership]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param permissionSlug The permission slug to filter by. Only child resources where the organization membership has this permission are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param parentResourceId The WorkOS ID of the parent resource. Provide this or both `parent_resource_external_id` and `parent_resource_type_slug`, but not both. Mutually exclusive with `parent_resource_type_slug` and `parent_resource_external_id`.
     * @param parentResourceTypeSlug The slug of the parent resource type. Must be provided together with `parent_resource_external_id`. Required with `parent_resource_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param parentResourceExternalId The application-specific external identifier of the parent resource. Must be provided together with `parent_resource_type_slug`. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuthorizationResource` values.
     */
    public fun listResourcesForMembershipAutoPaging(
        organizationMembershipId: String,
        permissionSlug: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        parentResourceId: String? = null,
        parentResourceTypeSlug: String? = null,
        parentResourceExternalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuthorizationResource> =
        autoPagingFlow { cursor ->
            listResourcesForMembership(
                organizationMembershipId = organizationMembershipId,
                permissionSlug = permissionSlug,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                parentResourceId = parentResourceId,
                parentResourceTypeSlug = parentResourceTypeSlug,
                parentResourceExternalId = parentResourceExternalId,
                requestOptions = requestOptions,
            )
        }

    /**
     * List effective permissions for an organization membership on a resource
     *
     * Returns all permissions the organization membership effectively has on a resource, including permissions inherited through roles assigned to ancestor resources.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param resourceId The ID of the authorization resource.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuthorizationPermission>` returned by the API.
     */
    public suspend fun listEffectivePermissions(
        organizationMembershipId: String,
        resourceId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuthorizationPermission> {
        val path = "authorization/organization_memberships/${PathEncoding.segment(
            organizationMembershipId,
        )}/resources/${PathEncoding.segment(resourceId)}/permissions"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<AuthorizationPermission>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listEffectivePermissions]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param resourceId The ID of the authorization resource.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuthorizationPermission` values.
     */
    public fun listEffectivePermissionsAutoPaging(
        organizationMembershipId: String,
        resourceId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuthorizationPermission> =
        autoPagingFlow { cursor ->
            listEffectivePermissions(
                organizationMembershipId = organizationMembershipId,
                resourceId = resourceId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * List effective permissions for an organization membership on a resource by external ID
     *
     * Returns all permissions the organization membership effectively has on a resource identified by its external ID, including permissions inherited through roles assigned to ancestor resources.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuthorizationPermission>` returned by the API.
     */
    public suspend fun listEffectivePermissionsByExternalId(
        organizationMembershipId: String,
        resourceTypeSlug: String,
        externalId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuthorizationPermission> {
        val path = "authorization/organization_memberships/${PathEncoding.segment(
            organizationMembershipId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}/permissions"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<AuthorizationPermission>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listEffectivePermissionsByExternalId]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuthorizationPermission` values.
     */
    public fun listEffectivePermissionsByExternalIdAutoPaging(
        organizationMembershipId: String,
        resourceTypeSlug: String,
        externalId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuthorizationPermission> =
        autoPagingFlow { cursor ->
            listEffectivePermissionsByExternalId(
                organizationMembershipId = organizationMembershipId,
                resourceTypeSlug = resourceTypeSlug,
                externalId = externalId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * List role assignments
     *
     * List all role assignments for an organization membership. This returns all roles that have been assigned to the user on resources, including organization-level and sub-resource roles.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param resourceId Filter assignments by the ID of the resource.
     * @param resourceExternalId Filter assignments by the external ID of the resource.
     * @param resourceTypeSlug Filter assignments by the slug of the resource type.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<UserRoleAssignment>` returned by the API.
     */
    public suspend fun listRoleAssignments(
        organizationMembershipId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserRoleAssignment> {
        val path = "authorization/organization_memberships/${PathEncoding.segment(organizationMembershipId)}/role_assignments"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        resourceId?.let { query.add(QueryParam("resource_id", it)) }
        resourceExternalId?.let { query.add(QueryParam("resource_external_id", it)) }
        resourceTypeSlug?.let { query.add(QueryParam("resource_type_slug", it)) }
        return transport.request<Page<UserRoleAssignment>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listRoleAssignments]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param resourceId Filter assignments by the ID of the resource.
     * @param resourceExternalId Filter assignments by the external ID of the resource.
     * @param resourceTypeSlug Filter assignments by the slug of the resource type.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `UserRoleAssignment` values.
     */
    public fun listRoleAssignmentsAutoPaging(
        organizationMembershipId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserRoleAssignment> =
        autoPagingFlow { cursor ->
            listRoleAssignments(
                organizationMembershipId = organizationMembershipId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                resourceId = resourceId,
                resourceExternalId = resourceExternalId,
                resourceTypeSlug = resourceTypeSlug,
                requestOptions = requestOptions,
            )
        }

    /**
     * Assign a role
     *
     * Assign a role to an organization membership on a specific resource.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param roleSlug The slug of the role to assign.
     * @param resourceId The ID of the resource. Mutually exclusive with `resource_external_id` and `resource_type_slug`.
     * @param resourceExternalId The external ID of the resource. Required with `resource_type_slug`. Mutually exclusive with `resource_id`.
     * @param resourceTypeSlug The resource type slug. Required with `resource_external_id`. Mutually exclusive with `resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `UserRoleAssignment` returned by the API.
     */
    public suspend fun assignRole(
        organizationMembershipId: String,
        roleSlug: String,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): UserRoleAssignment {
        val path = "authorization/organization_memberships/${PathEncoding.segment(organizationMembershipId)}/role_assignments"
        val payload = JsonBody()
        payload.set("role_slug", roleSlug)
        payload.set("resource_id", resourceId)
        payload.set("resource_external_id", resourceExternalId)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<UserRoleAssignment>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove a role assignment
     *
     * Remove a role assignment by role slug and resource.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param roleSlug The slug of the role to remove.
     * @param resourceId The ID of the resource. Mutually exclusive with `resource_external_id` and `resource_type_slug`.
     * @param resourceExternalId The external ID of the resource. Required with `resource_type_slug`. Mutually exclusive with `resource_id`.
     * @param resourceTypeSlug The resource type slug. Required with `resource_external_id`. Mutually exclusive with `resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun removeRole(
        organizationMembershipId: String,
        roleSlug: String,
        resourceId: String? = null,
        resourceExternalId: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/organization_memberships/${PathEncoding.segment(organizationMembershipId)}/role_assignments"
        val payload = JsonBody()
        payload.set("role_slug", roleSlug)
        payload.set("resource_id", resourceId)
        payload.set("resource_external_id", resourceExternalId)
        payload.set("resource_type_slug", resourceTypeSlug)
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove a role assignment by ID
     *
     * Remove a role assignment using its ID.
     *
     * @param organizationMembershipId The ID of the organization membership.
     * @param roleAssignmentId The ID of the role assignment to remove.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun removeRoleAssignment(
        organizationMembershipId: String,
        roleAssignmentId: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/organization_memberships/${PathEncoding.segment(
            organizationMembershipId,
        )}/role_assignments/${PathEncoding.segment(roleAssignmentId)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List custom roles
     *
     * Get a list of all roles that apply to an organization. This includes both environment roles and custom roles, returned in priority order.
     *
     * @param organizationId The ID of the organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `RoleList` returned by the API.
     */
    public suspend fun listOrganizationRoles(
        organizationId: String,
        requestOptions: RequestOptions? = null,
    ): RoleList {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles"
        return transport.request<RoleList>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Create a custom role
     *
     * Create a new custom role for this organization.
     *
     * @param organizationId The ID of the organization.
     * @param name A descriptive name for the role.
     * @param slug A unique identifier for the role within the organization. When provided, must begin with 'org-' and contain only lowercase letters, numbers, hyphens, and underscores. When omitted, a slug is auto-generated from the role name and a random suffix.
     * @param description An optional description of the role's purpose.
     * @param resourceTypeSlug The slug of the resource type the role is scoped to.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun createOrganizationRole(
        organizationId: String,
        name: String,
        slug: String? = null,
        description: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles"
        val payload = JsonBody()
        payload.set("slug", slug)
        payload.set("name", name)
        payload.set("description", description)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<Role>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a custom role
     *
     * Retrieve a role that applies to an organization by its slug. This can return either an environment role or a custom role.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun getOrganizationRole(
        organizationId: String,
        slug: String,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles/${PathEncoding.segment(slug)}"
        return transport.request<Role>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update a custom role
     *
     * Update an existing custom role. Only the fields provided in the request body will be updated.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param name A descriptive name for the role.
     * @param description An optional description of the role's purpose.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun updateOrganizationRole(
        organizationId: String,
        slug: String,
        name: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles/${PathEncoding.segment(slug)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        return transport.request<Role>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete a custom role
     *
     * Delete an existing custom role.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteOrganizationRole(
        organizationId: String,
        slug: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles/${PathEncoding.segment(slug)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Add a permission to a custom role
     *
     * Add a single permission to a custom role. If the permission is already assigned to the role, this operation has no effect.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param slug2 The slug of the permission to add to the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun addOrganizationRolePermission(
        organizationId: String,
        slug: String,
        slug2: String,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles/${PathEncoding.segment(slug)}/permissions"
        val payload = JsonBody()
        payload.set("slug", slug2)
        return transport.request<Role>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Set permissions for a custom role
     *
     * Replace all permissions on a custom role with the provided list.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param permissions The permission slugs to assign to the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun setOrganizationRolePermissions(
        organizationId: String,
        slug: String,
        permissions: List<String>,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/organizations/${PathEncoding.segment(organizationId)}/roles/${PathEncoding.segment(slug)}/permissions"
        val payload = JsonBody()
        payload.set("permissions", permissions)
        return transport.request<Role>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove a permission from a custom role
     *
     * Remove a single permission from a custom role by its slug.
     *
     * @param organizationId The ID of the organization.
     * @param slug The slug of the role.
     * @param permissionSlug The slug of the permission to remove.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun removeOrganizationRolePermission(
        organizationId: String,
        slug: String,
        permissionSlug: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/roles/${PathEncoding.segment(slug)}/permissions/${PathEncoding.segment(permissionSlug)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Get a resource by external ID
     *
     * Retrieve the details of an authorization resource by its external ID, organization, and resource type. This is useful when you only have the external ID from your system and need to fetch the full resource details.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationResource` returned by the API.
     */
    public suspend fun getResourceByExternalId(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        requestOptions: RequestOptions? = null,
    ): AuthorizationResource {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}"
        return transport.request<AuthorizationResource>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update a resource by external ID
     *
     * Update an existing authorization resource using its external ID.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param name A display name for the resource.
     * @param description An optional description of the resource.
     * @param parentResourceId The ID of the parent resource. Mutually exclusive with `parent_resource_external_id` and `parent_resource_type_slug`.
     * @param parentResourceExternalId The external ID of the parent resource. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param parentResourceTypeSlug The resource type slug of the parent resource. Required with `parent_resource_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationResource` returned by the API.
     */
    public suspend fun updateResourceByExternalId(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        name: String? = null,
        description: String? = null,
        parentResourceId: String? = null,
        parentResourceExternalId: String? = null,
        parentResourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): AuthorizationResource {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        payload.set("parent_resource_id", parentResourceId)
        payload.set("parent_resource_external_id", parentResourceExternalId)
        payload.set("parent_resource_type_slug", parentResourceTypeSlug)
        return transport.request<AuthorizationResource>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete an authorization resource by external ID
     *
     * Delete an authorization resource by organization, resource type, and external ID. This also deletes all descendant resources.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param cascadeDelete If true, deletes all descendant resources and role assignments. If not set and the resource has children or assignments, the request will fail.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteResourceByExternalId(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        cascadeDelete: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}"
        val query = mutableListOf<QueryParam>()
        cascadeDelete?.let { query.add(QueryParam("cascade_delete", it.toString())) }
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List memberships for a resource by external ID
     *
     * Returns all organization memberships that have a specific permission on a resource, using the resource's external ID. This is useful for answering "Who can access this resource?" when you only have the external ID.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type this resource belongs to.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param permissionSlug The permission slug to filter by. Only users with this permission on the resource are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param assignment Filter by assignment type. Use "direct" for direct assignments only, or "indirect" to include inherited assignments.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<UserOrganizationMembershipBaseListData>` returned by the API.
     */
    public suspend fun listMembershipsForResourceByExternalId(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        permissionSlug: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        assignment: AuthorizationAssignment? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserOrganizationMembershipBaseListData> {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}/organization_memberships"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        query.add(QueryParam("permission_slug", permissionSlug))
        assignment?.let { query.add(QueryParam("assignment", it.rawValue)) }
        return transport.request<Page<UserOrganizationMembershipBaseListData>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listMembershipsForResourceByExternalId]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type this resource belongs to.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param permissionSlug The permission slug to filter by. Only users with this permission on the resource are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param assignment Filter by assignment type. Use "direct" for direct assignments only, or "indirect" to include inherited assignments.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `UserOrganizationMembershipBaseListData` values.
     */
    public fun listMembershipsForResourceByExternalIdAutoPaging(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        permissionSlug: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        assignment: AuthorizationAssignment? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserOrganizationMembershipBaseListData> =
        autoPagingFlow { cursor ->
            listMembershipsForResourceByExternalId(
                organizationId = organizationId,
                resourceTypeSlug = resourceTypeSlug,
                externalId = externalId,
                permissionSlug = permissionSlug,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                assignment = assignment,
                requestOptions = requestOptions,
            )
        }

    /**
     * List role assignments for a resource by external ID
     *
     * List all role assignments granted on a resource, identified by its external ID. Each assignment includes the organization membership it was granted to.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param roleSlug Filter assignments by the slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<UserRoleAssignment>` returned by the API.
     */
    public suspend fun listRoleAssignmentsForResourceByExternalId(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        roleSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserRoleAssignment> {
        val path = "authorization/organizations/${PathEncoding.segment(
            organizationId,
        )}/resources/${PathEncoding.segment(resourceTypeSlug)}/${PathEncoding.segment(externalId)}/role_assignments"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        roleSlug?.let { query.add(QueryParam("role_slug", it)) }
        return transport.request<Page<UserRoleAssignment>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listRoleAssignmentsForResourceByExternalId]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId The ID of the organization that owns the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param externalId An identifier you provide to reference the resource in your system.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param roleSlug Filter assignments by the slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `UserRoleAssignment` values.
     */
    public fun listRoleAssignmentsForResourceByExternalIdAutoPaging(
        organizationId: String,
        resourceTypeSlug: String,
        externalId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        roleSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserRoleAssignment> =
        autoPagingFlow { cursor ->
            listRoleAssignmentsForResourceByExternalId(
                organizationId = organizationId,
                resourceTypeSlug = resourceTypeSlug,
                externalId = externalId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                roleSlug = roleSlug,
                requestOptions = requestOptions,
            )
        }

    /**
     * List resources
     *
     * Get a paginated list of authorization resources.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param organizationId Filter resources by organization ID.
     * @param resourceTypeSlug Filter resources by resource type slug.
     * @param resourceExternalId Filter resources by external ID.
     * @param parentResourceId Filter resources by parent resource ID. Mutually exclusive with `parent_resource_type_slug` and `parent_external_id`.
     * @param parentResourceTypeSlug Filter resources by parent resource type slug. Required with `parent_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param parentExternalId Filter resources by parent external ID. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuthorizationResource>` returned by the API.
     */
    public suspend fun listResources(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        organizationId: String? = null,
        resourceTypeSlug: String? = null,
        resourceExternalId: String? = null,
        parentResourceId: String? = null,
        parentResourceTypeSlug: String? = null,
        parentExternalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuthorizationResource> {
        val path = "authorization/resources"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        organizationId?.let { query.add(QueryParam("organization_id", it)) }
        resourceTypeSlug?.let { query.add(QueryParam("resource_type_slug", it)) }
        resourceExternalId?.let { query.add(QueryParam("resource_external_id", it)) }
        parentResourceId?.let { query.add(QueryParam("parent_resource_id", it)) }
        parentResourceTypeSlug?.let { query.add(QueryParam("parent_resource_type_slug", it)) }
        parentExternalId?.let { query.add(QueryParam("parent_external_id", it)) }
        return transport.request<Page<AuthorizationResource>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listResources]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param organizationId Filter resources by organization ID.
     * @param resourceTypeSlug Filter resources by resource type slug.
     * @param resourceExternalId Filter resources by external ID.
     * @param parentResourceId Filter resources by parent resource ID. Mutually exclusive with `parent_resource_type_slug` and `parent_external_id`.
     * @param parentResourceTypeSlug Filter resources by parent resource type slug. Required with `parent_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param parentExternalId Filter resources by parent external ID. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuthorizationResource` values.
     */
    public fun listResourcesAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        organizationId: String? = null,
        resourceTypeSlug: String? = null,
        resourceExternalId: String? = null,
        parentResourceId: String? = null,
        parentResourceTypeSlug: String? = null,
        parentExternalId: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuthorizationResource> =
        autoPagingFlow { cursor ->
            listResources(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                organizationId = organizationId,
                resourceTypeSlug = resourceTypeSlug,
                resourceExternalId = resourceExternalId,
                parentResourceId = parentResourceId,
                parentResourceTypeSlug = parentResourceTypeSlug,
                parentExternalId = parentExternalId,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create an authorization resource
     *
     * Create a new authorization resource.
     *
     * @param externalId An external identifier for the resource.
     * @param name A display name for the resource.
     * @param resourceTypeSlug The slug of the resource type.
     * @param organizationId The ID of the organization this resource belongs to.
     * @param description An optional description of the resource.
     * @param parentResourceId The ID of the parent resource. Mutually exclusive with `parent_resource_external_id` and `parent_resource_type_slug`.
     * @param parentResourceExternalId The external ID of the parent resource. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param parentResourceTypeSlug The resource type slug of the parent resource. Required with `parent_resource_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationResource` returned by the API.
     */
    public suspend fun createResource(
        externalId: String,
        name: String,
        resourceTypeSlug: String,
        organizationId: String,
        description: String? = null,
        parentResourceId: String? = null,
        parentResourceExternalId: String? = null,
        parentResourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): AuthorizationResource {
        val path = "authorization/resources"
        val payload = JsonBody()
        payload.set("external_id", externalId)
        payload.set("name", name)
        payload.set("description", description)
        payload.set("resource_type_slug", resourceTypeSlug)
        payload.set("organization_id", organizationId)
        payload.set("parent_resource_id", parentResourceId)
        payload.set("parent_resource_external_id", parentResourceExternalId)
        payload.set("parent_resource_type_slug", parentResourceTypeSlug)
        return transport.request<AuthorizationResource>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a resource
     *
     * Retrieve the details of an authorization resource by its ID.
     *
     * @param resourceId The ID of the authorization resource.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationResource` returned by the API.
     */
    public suspend fun getResource(
        resourceId: String,
        requestOptions: RequestOptions? = null,
    ): AuthorizationResource {
        val path = "authorization/resources/${PathEncoding.segment(resourceId)}"
        return transport.request<AuthorizationResource>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update a resource
     *
     * Update an existing authorization resource.
     *
     * @param resourceId The ID of the authorization resource.
     * @param name A display name for the resource.
     * @param description An optional description of the resource.
     * @param parentResourceId The ID of the parent resource. Mutually exclusive with `parent_resource_external_id` and `parent_resource_type_slug`.
     * @param parentResourceExternalId The external ID of the parent resource. Required with `parent_resource_type_slug`. Mutually exclusive with `parent_resource_id`.
     * @param parentResourceTypeSlug The resource type slug of the parent resource. Required with `parent_resource_external_id`. Mutually exclusive with `parent_resource_id`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationResource` returned by the API.
     */
    public suspend fun updateResource(
        resourceId: String,
        name: String? = null,
        description: String? = null,
        parentResourceId: String? = null,
        parentResourceExternalId: String? = null,
        parentResourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): AuthorizationResource {
        val path = "authorization/resources/${PathEncoding.segment(resourceId)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        payload.set("parent_resource_id", parentResourceId)
        payload.set("parent_resource_external_id", parentResourceExternalId)
        payload.set("parent_resource_type_slug", parentResourceTypeSlug)
        return transport.request<AuthorizationResource>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete an authorization resource
     *
     * Delete an authorization resource and all its descendants.
     *
     * @param resourceId The ID of the authorization resource.
     * @param cascadeDelete If true, deletes all descendant resources and role assignments. If not set and the resource has children or assignments, the request will fail.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deleteResource(
        resourceId: String,
        cascadeDelete: Boolean? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/resources/${PathEncoding.segment(resourceId)}"
        val query = mutableListOf<QueryParam>()
        cascadeDelete?.let { query.add(QueryParam("cascade_delete", it.toString())) }
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List organization memberships for resource
     *
     * Returns all organization memberships that have a specific permission on a resource instance. This is useful for answering "Who can access this resource?".
     *
     * @param resourceId The ID of the authorization resource.
     * @param permissionSlug The permission slug to filter by. Only users with this permission on the resource are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param assignment Filter by assignment type. Use `direct` for direct assignments only, or `indirect` to include inherited assignments.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<UserOrganizationMembershipBaseListData>` returned by the API.
     */
    public suspend fun listMembershipsForResource(
        resourceId: String,
        permissionSlug: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        assignment: AuthorizationAssignment? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserOrganizationMembershipBaseListData> {
        val path = "authorization/resources/${PathEncoding.segment(resourceId)}/organization_memberships"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        query.add(QueryParam("permission_slug", permissionSlug))
        assignment?.let { query.add(QueryParam("assignment", it.rawValue)) }
        return transport.request<Page<UserOrganizationMembershipBaseListData>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listMembershipsForResource]: emits every item across successive pages as the flow is collected.
     *
     * @param resourceId The ID of the authorization resource.
     * @param permissionSlug The permission slug to filter by. Only users with this permission on the resource are returned.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param assignment Filter by assignment type. Use `direct` for direct assignments only, or `indirect` to include inherited assignments.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `UserOrganizationMembershipBaseListData` values.
     */
    public fun listMembershipsForResourceAutoPaging(
        resourceId: String,
        permissionSlug: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        assignment: AuthorizationAssignment? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserOrganizationMembershipBaseListData> =
        autoPagingFlow { cursor ->
            listMembershipsForResource(
                resourceId = resourceId,
                permissionSlug = permissionSlug,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                assignment = assignment,
                requestOptions = requestOptions,
            )
        }

    /**
     * List role assignments for a resource
     *
     * List all role assignments granted on a specific resource instance. Each assignment includes the organization membership it was granted to.
     *
     * @param resourceId The ID of the authorization resource.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param roleSlug Filter assignments by the slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<UserRoleAssignment>` returned by the API.
     */
    public suspend fun listRoleAssignmentsForResource(
        resourceId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        roleSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserRoleAssignment> {
        val path = "authorization/resources/${PathEncoding.segment(resourceId)}/role_assignments"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        roleSlug?.let { query.add(QueryParam("role_slug", it)) }
        return transport.request<Page<UserRoleAssignment>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listRoleAssignmentsForResource]: emits every item across successive pages as the flow is collected.
     *
     * @param resourceId The ID of the authorization resource.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param roleSlug Filter assignments by the slug of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `UserRoleAssignment` values.
     */
    public fun listRoleAssignmentsForResourceAutoPaging(
        resourceId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        roleSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserRoleAssignment> =
        autoPagingFlow { cursor ->
            listRoleAssignmentsForResource(
                resourceId = resourceId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                roleSlug = roleSlug,
                requestOptions = requestOptions,
            )
        }

    /**
     * List environment roles
     *
     * List all environment roles in priority order.
     *
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `RoleList` returned by the API.
     */
    public suspend fun listEnvironmentRoles(requestOptions: RequestOptions? = null): RoleList {
        val path = "authorization/roles"
        return transport.request<RoleList>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Create an environment role
     *
     * Create a new environment role.
     *
     * @param slug A unique slug for the role.
     * @param name A descriptive name for the role.
     * @param description An optional description of the role.
     * @param resourceTypeSlug The slug of the resource type the role is scoped to.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun createEnvironmentRole(
        slug: String,
        name: String,
        description: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/roles"
        val payload = JsonBody()
        payload.set("slug", slug)
        payload.set("name", name)
        payload.set("description", description)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<Role>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get an environment role
     *
     * Get an environment role by its slug.
     *
     * @param slug The slug of the environment role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun getEnvironmentRole(
        slug: String,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/roles/${PathEncoding.segment(slug)}"
        return transport.request<Role>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update an environment role
     *
     * Update an existing environment role.
     *
     * @param slug The slug of the environment role.
     * @param name A descriptive name for the role.
     * @param description An optional description of the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun updateEnvironmentRole(
        slug: String,
        name: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/roles/${PathEncoding.segment(slug)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        return transport.request<Role>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Add a permission to an environment role
     *
     * Add a single permission to an environment role. If the permission is already assigned to the role, this operation has no effect.
     *
     * @param slug The slug of the environment role.
     * @param slug2 The slug of the permission to add to the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun addEnvironmentRolePermission(
        slug: String,
        slug2: String,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/roles/${PathEncoding.segment(slug)}/permissions"
        val payload = JsonBody()
        payload.set("slug", slug2)
        return transport.request<Role>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Set permissions for an environment role
     *
     * Replace all permissions on an environment role with the provided list.
     *
     * @param slug The slug of the environment role.
     * @param permissions The permission slugs to assign to the role.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Role` returned by the API.
     */
    public suspend fun setEnvironmentRolePermissions(
        slug: String,
        permissions: List<String>,
        requestOptions: RequestOptions? = null,
    ): Role {
        val path = "authorization/roles/${PathEncoding.segment(slug)}/permissions"
        val payload = JsonBody()
        payload.set("permissions", permissions)
        return transport.request<Role>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * List permissions
     *
     * Get a list of all permissions in your WorkOS environment.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuthorizationPermission>` returned by the API.
     */
    public suspend fun listPermissions(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuthorizationPermission> {
        val path = "authorization/permissions"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<AuthorizationPermission>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listPermissions]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuthorizationPermission` values.
     */
    public fun listPermissionsAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuthorizationPermission> =
        autoPagingFlow { cursor ->
            listPermissions(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create a permission
     *
     * Create a new permission in your WorkOS environment. The permission can then be assigned to environment roles and custom roles.
     *
     * @param slug A unique key to reference the permission. Must be lowercase and contain only letters, numbers, hyphens, underscores, colons, periods, and asterisks.
     * @param name A descriptive name for the Permission.
     * @param description An optional description of the Permission.
     * @param resourceTypeSlug The slug of the resource type this permission is scoped to.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Permission` returned by the API.
     */
    public suspend fun createPermission(
        slug: String,
        name: String,
        description: String? = null,
        resourceTypeSlug: String? = null,
        requestOptions: RequestOptions? = null,
    ): Permission {
        val path = "authorization/permissions"
        val payload = JsonBody()
        payload.set("slug", slug)
        payload.set("name", name)
        payload.set("description", description)
        payload.set("resource_type_slug", resourceTypeSlug)
        return transport.request<Permission>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a permission
     *
     * Retrieve a permission by its unique slug.
     *
     * @param slug A unique key to reference the permission. Must be lowercase and contain only letters, numbers, hyphens, underscores, colons, periods, and asterisks.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationPermission` returned by the API.
     */
    public suspend fun getPermission(
        slug: String,
        requestOptions: RequestOptions? = null,
    ): AuthorizationPermission {
        val path = "authorization/permissions/${PathEncoding.segment(slug)}"
        return transport.request<AuthorizationPermission>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update a permission
     *
     * Update an existing permission. Only the fields provided in the request body will be updated.
     *
     * @param slug A unique key to reference the permission. Must be lowercase and contain only letters, numbers, hyphens, underscores, colons, periods, and asterisks.
     * @param name A descriptive name for the Permission.
     * @param description An optional description of the Permission.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuthorizationPermission` returned by the API.
     */
    public suspend fun updatePermission(
        slug: String,
        name: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): AuthorizationPermission {
        val path = "authorization/permissions/${PathEncoding.segment(slug)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        return transport.request<AuthorizationPermission>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete a permission
     *
     * Delete an existing permission. System permissions cannot be deleted.
     *
     * @param slug A unique key to reference the permission. Must be lowercase and contain only letters, numbers, hyphens, underscores, colons, periods, and asterisks.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun deletePermission(
        slug: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "authorization/permissions/${PathEncoding.segment(slug)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }
}
