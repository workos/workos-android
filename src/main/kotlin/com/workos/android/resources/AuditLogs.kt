// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.AuditLogAction
import com.workos.android.models.AuditLogEvent
import com.workos.android.models.AuditLogEventCreateResponse
import com.workos.android.models.AuditLogExport
import com.workos.android.models.AuditLogSchema
import com.workos.android.models.AuditLogSchemaActorInput
import com.workos.android.models.AuditLogSchemaTargetInput
import com.workos.android.models.AuditLogsRetention
import kotlinx.coroutines.flow.Flow
import kotlinx.datetime.Instant
import kotlinx.serialization.json.JsonElement

/** Operations for the AuditLogs API. */
public class AuditLogs internal constructor(
    private val transport: Transport,
) {
    /**
     * Get Retention
     *
     * Get the configured event retention period for the given Organization.
     *
     * @param id Unique identifier of the Organization.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogsRetention` returned by the API.
     */
    public suspend fun getOrganizationAuditLogsRetention(
        id: String,
        requestOptions: RequestOptions? = null,
    ): AuditLogsRetention {
        val path = "organizations/${PathEncoding.segment(id)}/audit_logs_retention"
        return transport.request<AuditLogsRetention>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Set Retention
     *
     * Set the event retention period for the given Organization.
     *
     * @param id Unique identifier of the Organization.
     * @param retentionPeriodInDays The number of days Audit Log events will be retained. Valid values are `30` and `365`.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogsRetention` returned by the API.
     */
    public suspend fun updateOrganizationAuditLogsRetention(
        id: String,
        retentionPeriodInDays: Long,
        requestOptions: RequestOptions? = null,
    ): AuditLogsRetention {
        val path = "organizations/${PathEncoding.segment(id)}/audit_logs_retention"
        val payload = JsonBody()
        payload.set("retention_period_in_days", retentionPeriodInDays)
        return transport.request<AuditLogsRetention>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * List Actions
     *
     * Get a list of all Audit Log actions in the current environment.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuditLogAction>` returned by the API.
     */
    public suspend fun listActions(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuditLogAction> {
        val path = "audit_logs/actions"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<AuditLogAction>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listActions]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuditLogAction` values.
     */
    public fun listActionsAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuditLogAction> =
        autoPagingFlow { cursor ->
            listActions(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * List Schemas
     *
     * Get a list of all schemas for the Audit Logs action identified by `:name`.
     *
     * @param actionName The name of the Audit Log action.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<AuditLogSchema>` returned by the API.
     */
    public suspend fun listActionSchemas(
        actionName: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<AuditLogSchema> {
        val path = "audit_logs/actions/${PathEncoding.segment(actionName)}/schemas"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<AuditLogSchema>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listActionSchemas]: emits every item across successive pages as the flow is collected.
     *
     * @param actionName The name of the Audit Log action.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `AuditLogSchema` values.
     */
    public fun listActionSchemasAutoPaging(
        actionName: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<AuditLogSchema> =
        autoPagingFlow { cursor ->
            listActionSchemas(
                actionName = actionName,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create Schema
     *
     * Creates a new Audit Log schema used to validate the payload of incoming Audit Log Events. If the `action` does not exist, it will also be created.
     *
     * @param actionName The name of the Audit Log action.
     * @param targets The list of targets for the schema.
     * @param actor The metadata schema for the actor.
     * @param metadata Optional JSON schema for event metadata.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogSchema` returned by the API.
     */
    public suspend fun createSchema(
        actionName: String,
        targets: List<AuditLogSchemaTargetInput>,
        actor: AuditLogSchemaActorInput? = null,
        metadata: Map<String, JsonElement>? = null,
        requestOptions: RequestOptions? = null,
    ): AuditLogSchema {
        val path = "audit_logs/actions/${PathEncoding.segment(actionName)}/schemas"
        val payload = JsonBody()
        payload.set("actor", actor)
        payload.set("targets", targets)
        payload.set("metadata", metadata)
        return transport.request<AuditLogSchema>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Create Event
     *
     * Create an Audit Log Event.
     *
     * This API supports idempotency which guarantees that performing the same operation multiple times will have the same result as if the operation were performed only once. This is handy in situations where you may need to retry a request due to a failure or prevent accidental duplicate requests from creating more than one resource.
     *
     * To achieve idempotency, you can add `Idempotency-Key` request header to a Create Event request with a unique string as the value. Each subsequent request matching this unique string will return the same response. We suggest using [v4 UUIDs](https://en.wikipedia.org/wiki/Universally_unique_identifier) for idempotency keys to avoid collisions.
     *
     * Idempotency keys expire after 24 hours. The API will generate a new response if you submit a request with an expired key.
     *
     * @param organizationId The unique ID of the Organization.
     * @param event The audit log event to create.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogEventCreateResponse` returned by the API.
     */
    public suspend fun createEvent(
        organizationId: String,
        event: AuditLogEvent,
        requestOptions: RequestOptions? = null,
    ): AuditLogEventCreateResponse {
        val path = "audit_logs/events"
        val payload = JsonBody()
        payload.set("organization_id", organizationId)
        payload.set("event", event)
        return transport.request<AuditLogEventCreateResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Create Export
     *
     * Create an Audit Log Export. Exports are scoped to a single organization within a specified date range.
     *
     * @param organizationId The unique ID of the Organization.
     * @param rangeStart ISO-8601 value for start of the export range.
     * @param rangeEnd ISO-8601 value for end of the export range.
     * @param actions List of actions to filter against.
     * @param actors Deprecated. Use `actor_names` instead.
     * @param actorNames List of actor names to filter against.
     * @param actorIds List of actor IDs to filter against.
     * @param targets List of target types to filter against.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogExport` returned by the API.
     */
    public suspend fun createExport(
        organizationId: String,
        rangeStart: Instant,
        rangeEnd: Instant,
        actions: List<String>? = null,
        actors: List<String>? = null,
        actorNames: List<String>? = null,
        actorIds: List<String>? = null,
        targets: List<String>? = null,
        requestOptions: RequestOptions? = null,
    ): AuditLogExport {
        val path = "audit_logs/exports"
        val payload = JsonBody()
        payload.set("organization_id", organizationId)
        payload.set("range_start", rangeStart)
        payload.set("range_end", rangeEnd)
        payload.set("actions", actions)
        payload.set("actors", actors)
        payload.set("actor_names", actorNames)
        payload.set("actor_ids", actorIds)
        payload.set("targets", targets)
        return transport.request<AuditLogExport>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get Export
     *
     * Get an Audit Log Export. The URL will expire after 10 minutes. If the export is needed again at a later time, refetching the export will regenerate the URL.
     *
     * @param auditLogExportId The unique ID of the Audit Log Export.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `AuditLogExport` returned by the API.
     */
    public suspend fun getExport(
        auditLogExportId: String,
        requestOptions: RequestOptions? = null,
    ): AuditLogExport {
        val path = "audit_logs/exports/${PathEncoding.segment(auditLogExportId)}"
        return transport.request<AuditLogExport>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }
}
