// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.Group
import com.workos.android.models.UserOrganizationMembershipBaseListData
import kotlinx.coroutines.flow.Flow

/** Operations for the Groups API. */
public class Groups internal constructor(
    private val transport: Transport,
) {
    /**
     * List groups
     *
     * Get a paginated list of groups within an organization.
     *
     * @param organizationId The ID of the organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Page<Group>` returned by the API.
     */
    public suspend fun listOrganizationGroups(
        organizationId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Group> {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<Group>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listOrganizationGroups]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId The ID of the organization.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return A cold flow of `Group` values.
     */
    public fun listOrganizationGroupsAutoPaging(
        organizationId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<Group> =
        autoPagingFlow { cursor ->
            listOrganizationGroups(
                organizationId = organizationId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Create a group
     *
     * Create a new group within an organization.
     *
     * @param organizationId The ID of the organization.
     * @param name The name of the Group.
     * @param description An optional description of the Group.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Group` returned by the API.
     */
    public suspend fun createOrganizationGroup(
        organizationId: String,
        name: String,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): Group {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        return transport.request<Group>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Get a group
     *
     * Retrieve a group by its ID within an organization.
     *
     * @param organizationId The ID of the organization.
     * @param groupId The ID of the group.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Group` returned by the API.
     */
    public suspend fun getOrganizationGroup(
        organizationId: String,
        groupId: String,
        requestOptions: RequestOptions? = null,
    ): Group {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups/${PathEncoding.segment(groupId)}"
        return transport.request<Group>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Update a group
     *
     * Update an existing group. Only the fields provided in the request body will be updated.
     *
     * @param organizationId The ID of the organization.
     * @param groupId The ID of the group.
     * @param name The name of the Group.
     * @param description An optional description of the Group.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Group` returned by the API.
     */
    public suspend fun updateOrganizationGroup(
        organizationId: String,
        groupId: String,
        name: String? = null,
        description: String? = null,
        requestOptions: RequestOptions? = null,
    ): Group {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups/${PathEncoding.segment(groupId)}"
        val payload = JsonBody()
        payload.set("name", name)
        payload.set("description", description)
        return transport.request<Group>(
            method = "PATCH",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Delete a group
     *
     * Delete a group from an organization.
     *
     * @param organizationId The ID of the organization.
     * @param groupId The ID of the group.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     */
    public suspend fun deleteOrganizationGroup(
        organizationId: String,
        groupId: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups/${PathEncoding.segment(groupId)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List Group members
     *
     * Get a list of organization memberships in a group.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param groupId Unique identifier of the Group.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Page<UserOrganizationMembershipBaseListData>` returned by the API.
     */
    public suspend fun listOrganizationMemberships(
        organizationId: String,
        groupId: String,
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Page<UserOrganizationMembershipBaseListData> {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups/${PathEncoding.segment(groupId)}/organization-memberships"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        return transport.request<Page<UserOrganizationMembershipBaseListData>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listOrganizationMemberships]: emits every item across successive pages as the flow is collected.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param groupId Unique identifier of the Group.
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return A cold flow of `UserOrganizationMembershipBaseListData` values.
     */
    public fun listOrganizationMembershipsAutoPaging(
        organizationId: String,
        groupId: String,
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<UserOrganizationMembershipBaseListData> =
        autoPagingFlow { cursor ->
            listOrganizationMemberships(
                organizationId = organizationId,
                groupId = groupId,
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                requestOptions = requestOptions,
            )
        }

    /**
     * Add a member to a Group
     *
     * Add an organization membership to a group.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param groupId Unique identifier of the Group.
     * @param organizationMembershipId The ID of the Organization Membership to add to the group.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `Group` returned by the API.
     */
    public suspend fun createOrganizationMembership(
        organizationId: String,
        groupId: String,
        organizationMembershipId: String,
        requestOptions: RequestOptions? = null,
    ): Group {
        val path = "organizations/${PathEncoding.segment(organizationId)}/groups/${PathEncoding.segment(groupId)}/organization-memberships"
        val payload = JsonBody()
        payload.set("organization_membership_id", organizationMembershipId)
        return transport.request<Group>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove a member from a Group
     *
     * Remove an organization membership from a group.
     *
     * @param organizationId Unique identifier of the Organization.
     * @param groupId Unique identifier of the Group.
     * @param omId Unique identifier of the Organization Membership.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     */
    public suspend fun deleteOrganizationMembership(
        organizationId: String,
        groupId: String,
        omId: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "organizations/${PathEncoding.segment(
            organizationId,
        )}/groups/${PathEncoding.segment(groupId)}/organization-memberships/${PathEncoding.segment(omId)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }
}
