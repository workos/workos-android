// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.RequestOptions
import com.workos.android.enums.RadarListAction
import com.workos.android.enums.RadarListType
import com.workos.android.enums.RadarStandaloneAssessRequestAction
import com.workos.android.enums.RadarStandaloneAssessRequestAuthMethod
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.Transport
import com.workos.android.models.RadarListEntryAlreadyPresentResponse
import com.workos.android.models.RadarStandaloneResponse

/** Operations for the Radar API. */
public class Radar internal constructor(
    private val transport: Transport,
) {
    /**
     * Create an attempt
     *
     * Assess a request for risk using the Radar engine and receive a verdict.
     *
     * @param ipAddress The IP address of the request to assess.
     * @param userAgent The user agent string of the request to assess.
     * @param email The email address of the user making the request.
     * @param authMethod The authentication method being used.
     * @param action The action being performed.
     * @param signalsId An optional Radar signals ID for the request.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `RadarStandaloneResponse` returned by the API.
     */
    public suspend fun createAttempt(
        ipAddress: String,
        userAgent: String,
        email: String,
        authMethod: RadarStandaloneAssessRequestAuthMethod,
        action: RadarStandaloneAssessRequestAction,
        signalsId: String? = null,
        requestOptions: RequestOptions? = null,
    ): RadarStandaloneResponse {
        val path = "radar/attempts"
        val payload = JsonBody()
        payload.set("ip_address", ipAddress)
        payload.set("user_agent", userAgent)
        payload.set("email", email)
        payload.set("auth_method", authMethod)
        payload.set("action", action)
        payload.set("signals_id", signalsId)
        return transport.request<RadarStandaloneResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Update a Radar attempt
     *
     * You may optionally inform Radar that an authentication attempt or challenge was successful using this endpoint. Some Radar controls depend on tracking recent successful attempts, such as impossible travel.
     *
     * @param id The unique identifier of the Radar attempt to update.
     * @param challengeStatus Set to `"success"` to mark the challenge as completed.
     * @param attemptStatus Set to `"success"` to mark the authentication attempt as successful.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     */
    public suspend fun updateAttempt(
        id: String,
        challengeStatus: String? = null,
        attemptStatus: String? = null,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "radar/attempts/${PathEncoding.segment(id)}"
        val payload = JsonBody()
        payload.set("challenge_status", challengeStatus)
        payload.set("attempt_status", attemptStatus)
        transport.requestVoid(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Add an entry to a Radar list
     *
     * Add an entry to a Radar list.
     *
     * @param type The type of the Radar list (e.g. ip_address, domain, email).
     * @param action The list action indicating whether to add the entry to the allow or block list.
     * @param entry The value to add to the list. Must match the format of the list type (e.g. a valid IP address for `ip_address`, a valid email for `email`).
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     * @return The `RadarListEntryAlreadyPresentResponse` returned by the API.
     */
    public suspend fun addListEntry(
        type: RadarListType,
        action: RadarListAction,
        entry: String,
        requestOptions: RequestOptions? = null,
    ): RadarListEntryAlreadyPresentResponse {
        val path = "radar/lists/${PathEncoding.segment(type.rawValue)}/${PathEncoding.segment(action.rawValue)}"
        val payload = JsonBody()
        payload.set("entry", entry)
        return transport.request<RadarListEntryAlreadyPresentResponse>(
            method = "POST",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }

    /**
     * Remove an entry from a Radar list
     *
     * Remove an entry from a Radar list.
     *
     * @param type The type of the Radar list (e.g. ip_address, domain, email).
     * @param action The list action indicating whether to remove the entry from the allow or block list.
     * @param entry The value to remove from the list. Must match an existing entry.
     * @param requestOptions Per-request overrides (idempotency key, API key, headers, timeout).
     */
    public suspend fun removeListEntry(
        type: RadarListType,
        action: RadarListAction,
        entry: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "radar/lists/${PathEncoding.segment(type.rawValue)}/${PathEncoding.segment(action.rawValue)}"
        val payload = JsonBody()
        payload.set("entry", entry)
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }
}
