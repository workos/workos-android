// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.RequestOptions
import com.workos.android.internal.JsonBody
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.Transport
import com.workos.android.models.DataIntegrationConfigurationListResponse
import com.workos.android.models.DataIntegrationConfigurationResponse

/** Operations for the PipesProvider API. */
public class PipesProvider internal constructor(
    private val transport: Transport,
) {
    /**
     * List providers for an organization
     *
     * Returns a list of all providers available to the specified organization, along with any configured custom OAuth scopes, enabled state, and organization-managed credentials where applicable.
     *
     * @param organizationId An [Organization](https://workos.com/docs/reference/organization) identifier to list provider configurations for.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `DataIntegrationConfigurationListResponse` returned by the API.
     */
    public suspend fun listOrganizationDataIntegrationConfigurations(
        organizationId: String,
        requestOptions: RequestOptions? = null,
    ): DataIntegrationConfigurationListResponse {
        val path = "organizations/${PathEncoding.segment(organizationId)}/data_integration_configurations"
        return transport.request<DataIntegrationConfigurationListResponse>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Configure a provider for an organization
     *
     * Creates or updates an organization's provider configuration. Use this endpoint to enable or disable a provider, set custom OAuth scopes, or supply organization-managed OAuth credentials.
     *
     * @param organizationId An [Organization](https://workos.com/docs/reference/organization) identifier to configure the provider for.
     * @param slug The slug identifier of the provider to configure (e.g., `github`, `slack`, `notion`).
     * @param enabled Whether the provider is enabled for the organization.
     * @param scopes The OAuth scopes to request for the organization. Pass `null` to inherit the provider scopes.
     * @param clientId The OAuth client ID of the organization's own application. Must be provided together with `client_secret`, and only for providers whose credentials are supplied by the organization.
     * @param clientSecret The OAuth client secret of the organization's own application. Must be provided together with `client_id`.
     * @param config Provider-specific config values to set for the organization, keyed by config field. Only fields the provider declares are accepted, and each value must match that field's pattern. Accepted only for providers whose credentials are organization-managed; for shared or custom credential providers, config belongs on the integration itself (via the data-integrations API) and supplying it here is rejected.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `DataIntegrationConfigurationResponse` returned by the API.
     */
    public suspend fun updateOrganizationDataIntegrationConfiguration(
        organizationId: String,
        slug: String,
        enabled: Boolean? = null,
        scopes: List<String>? = null,
        clientId: String? = null,
        clientSecret: String? = null,
        config: Map<String, String>? = null,
        requestOptions: RequestOptions? = null,
    ): DataIntegrationConfigurationResponse {
        val path = "organizations/${PathEncoding.segment(organizationId)}/data_integration_configurations/${PathEncoding.segment(slug)}"
        val payload = JsonBody()
        payload.set("enabled", enabled)
        payload.set("scopes", scopes)
        payload.set("client_id", clientId)
        payload.set("client_secret", clientSecret)
        payload.set("config", config)
        return transport.request<DataIntegrationConfigurationResponse>(
            method = "PUT",
            path = path,
            query = emptyList(),
            body = payload,
            options = requestOptions,
        )
    }
}
