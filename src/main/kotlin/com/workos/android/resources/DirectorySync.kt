// This file is auto-generated by oagen. Do not edit.

package com.workos.android.resources

import com.workos.android.Page
import com.workos.android.RequestOptions
import com.workos.android.enums.PaginationOrder
import com.workos.android.internal.PathEncoding
import com.workos.android.internal.QueryParam
import com.workos.android.internal.Transport
import com.workos.android.internal.autoPagingFlow
import com.workos.android.models.Directory
import com.workos.android.models.DirectoryGroup
import com.workos.android.models.DirectoryUserWithGroups
import kotlinx.coroutines.flow.Flow

/** Operations for the DirectorySync API. */
public class DirectorySync internal constructor(
    private val transport: Transport,
) {
    /**
     * List Directories
     *
     * Get a list of all of your existing directories matching the criteria specified.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param organizationId Filter Directories by their associated organization.
     * @param search Searchable text to match against Directory names.
     * @param domain Deprecated. Filter Directories by their associated domain.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<Directory>` returned by the API.
     */
    public suspend fun list(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        organizationId: String? = null,
        search: String? = null,
        domain: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<Directory> {
        val path = "directories"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        organizationId?.let { query.add(QueryParam("organization_id", it)) }
        search?.let { query.add(QueryParam("search", it)) }
        domain?.let { query.add(QueryParam("domain", it)) }
        return transport.request<Page<Directory>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [list]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time.
     * @param organizationId Filter Directories by their associated organization.
     * @param search Searchable text to match against Directory names.
     * @param domain Deprecated. Filter Directories by their associated domain.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `Directory` values.
     */
    public fun listAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        organizationId: String? = null,
        search: String? = null,
        domain: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<Directory> =
        autoPagingFlow { cursor ->
            list(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                organizationId = organizationId,
                search = search,
                domain = domain,
                requestOptions = requestOptions,
            )
        }

    /**
     * Get a Directory
     *
     * Get the details of an existing directory.
     *
     * @param id Unique identifier for the Directory.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Directory` returned by the API.
     */
    public suspend fun get(
        id: String,
        requestOptions: RequestOptions? = null,
    ): Directory {
        val path = "directories/${PathEncoding.segment(id)}"
        return transport.request<Directory>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Delete a Directory
     *
     * Permanently deletes an existing directory. It cannot be undone.
     *
     * @param id Unique identifier for the Directory.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     */
    public suspend fun delete(
        id: String,
        requestOptions: RequestOptions? = null,
    ) {
        val path = "directories/${PathEncoding.segment(id)}"
        transport.requestVoid(
            method = "DELETE",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List Directory Groups
     *
     * Get a list of all of existing directory groups matching the criteria specified.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param directory Unique identifier of the WorkOS Directory. This value can be obtained from the WorkOS dashboard or from the WorkOS API.
     * @param user Unique identifier of the WorkOS Directory User. This value can be obtained from the WorkOS API.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<DirectoryGroup>` returned by the API.
     */
    public suspend fun listGroups(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        directory: String? = null,
        user: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<DirectoryGroup> {
        val path = "directory_groups"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        directory?.let { query.add(QueryParam("directory", it)) }
        user?.let { query.add(QueryParam("user", it)) }
        return transport.request<Page<DirectoryGroup>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listGroups]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param directory Unique identifier of the WorkOS Directory. This value can be obtained from the WorkOS dashboard or from the WorkOS API.
     * @param user Unique identifier of the WorkOS Directory User. This value can be obtained from the WorkOS API.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `DirectoryGroup` values.
     */
    public fun listGroupsAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        directory: String? = null,
        user: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<DirectoryGroup> =
        autoPagingFlow { cursor ->
            listGroups(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                directory = directory,
                user = user,
                requestOptions = requestOptions,
            )
        }

    /**
     * Get a Directory Group
     *
     * Get the details of an existing Directory Group.
     *
     * @param id Unique identifier for the Directory Group.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `DirectoryGroup` returned by the API.
     */
    public suspend fun getGroup(
        id: String,
        requestOptions: RequestOptions? = null,
    ): DirectoryGroup {
        val path = "directory_groups/${PathEncoding.segment(id)}"
        return transport.request<DirectoryGroup>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }

    /**
     * List Directory Users
     *
     * Get a list of all of existing Directory Users matching the criteria specified.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param after An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param directory Unique identifier of the WorkOS Directory. This value can be obtained from the WorkOS dashboard or from the WorkOS API.
     * @param group Unique identifier of the WorkOS Directory Group. This value can be obtained from the WorkOS API.
     * @param idpId Filter Directory Users by the identity provider's unique identifier (`idp_id`). Requires the `directory` parameter to also be provided.
     * @param email Filter Directory Users by their primary email address. Requires the `directory` parameter to also be provided.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `Page<DirectoryUserWithGroups>` returned by the API.
     */
    public suspend fun listUsers(
        before: String? = null,
        after: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        directory: String? = null,
        group: String? = null,
        idpId: String? = null,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): Page<DirectoryUserWithGroups> {
        val path = "directory_users"
        val query = mutableListOf<QueryParam>()
        before?.let { query.add(QueryParam("before", it)) }
        after?.let { query.add(QueryParam("after", it)) }
        limit?.let { query.add(QueryParam("limit", it.toString())) }
        order?.let { query.add(QueryParam("order", it.rawValue)) }
        directory?.let { query.add(QueryParam("directory", it)) }
        group?.let { query.add(QueryParam("group", it)) }
        idpId?.let { query.add(QueryParam("idp_id", it)) }
        email?.let { query.add(QueryParam("email", it)) }
        return transport.request<Page<DirectoryUserWithGroups>>(
            method = "GET",
            path = path,
            query = query,
            body = null,
            options = requestOptions,
        )
    }

    /**
     * Auto-paginating variant of [listUsers]: emits every item across successive pages as the flow is collected.
     *
     * @param before An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
     * @param limit Upper limit on the number of objects to return, between `1` and `100`.
     * @param order Order the results by the creation time. Supported values are `"asc"` (ascending), `"desc"` (descending), and `"normal"` (descending with reversed cursor semantics where `before` fetches older records and `after` fetches newer records).
     * @param directory Unique identifier of the WorkOS Directory. This value can be obtained from the WorkOS dashboard or from the WorkOS API.
     * @param group Unique identifier of the WorkOS Directory Group. This value can be obtained from the WorkOS API.
     * @param idpId Filter Directory Users by the identity provider's unique identifier (`idp_id`). Requires the `directory` parameter to also be provided.
     * @param email Filter Directory Users by their primary email address. Requires the `directory` parameter to also be provided.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return A cold flow of `DirectoryUserWithGroups` values.
     */
    public fun listUsersAutoPaging(
        before: String? = null,
        limit: Long? = null,
        order: PaginationOrder? = null,
        directory: String? = null,
        group: String? = null,
        idpId: String? = null,
        email: String? = null,
        requestOptions: RequestOptions? = null,
    ): Flow<DirectoryUserWithGroups> =
        autoPagingFlow { cursor ->
            listUsers(
                before = before,
                after = cursor,
                limit = limit,
                order = order,
                directory = directory,
                group = group,
                idpId = idpId,
                email = email,
                requestOptions = requestOptions,
            )
        }

    /**
     * Get a Directory User
     *
     * Get the details of an existing Directory User.
     *
     * @param id Unique identifier for the Directory User.
     * @param requestOptions Per-request overrides: extra headers, timeout, retries, base URL, idempotency key.
     * @return The `DirectoryUserWithGroups` returned by the API.
     */
    public suspend fun getUser(
        id: String,
        requestOptions: RequestOptions? = null,
    ): DirectoryUserWithGroups {
        val path = "directory_users/${PathEncoding.segment(id)}"
        return transport.request<DirectoryUserWithGroups>(
            method = "GET",
            path = path,
            query = emptyList(),
            body = null,
            options = requestOptions,
        )
    }
}
