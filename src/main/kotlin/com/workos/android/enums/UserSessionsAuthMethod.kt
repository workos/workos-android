// This file is auto-generated by oagen. Do not edit.

package com.workos.android.enums

import kotlinx.serialization.KSerializer
import kotlinx.serialization.Serializable
import kotlinx.serialization.descriptors.PrimitiveKind
import kotlinx.serialization.descriptors.PrimitiveSerialDescriptor
import kotlinx.serialization.descriptors.SerialDescriptor
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

/** Enumeration of valid UserSessionsAuthMethod values. */
@Serializable(with = UserSessionsAuthMethod.Serializer::class)
public sealed class UserSessionsAuthMethod(
    public val rawValue: String,
) {
    public data object CrossAppAuth : UserSessionsAuthMethod("cross_app_auth")

    public data object ExternalAuth : UserSessionsAuthMethod("external_auth")

    public data object Impersonation : UserSessionsAuthMethod("impersonation")

    public data object MagicCode : UserSessionsAuthMethod("magic_code")

    public data object MigratedSession : UserSessionsAuthMethod("migrated_session")

    public data object OAuth : UserSessionsAuthMethod("oauth")

    public data object Passkey : UserSessionsAuthMethod("passkey")

    public data object Password : UserSessionsAuthMethod("password")

    public data object SSO : UserSessionsAuthMethod("sso")

    public data object Unknown2 : UserSessionsAuthMethod("unknown")

    /** A value not known at SDK generation time. */
    public data class Unknown(
        public val value: String,
    ) : UserSessionsAuthMethod(value)

    public companion object {
        public val allKnownValues: List<UserSessionsAuthMethod> =
            listOf(
                CrossAppAuth,
                ExternalAuth,
                Impersonation,
                MagicCode,
                MigratedSession,
                OAuth,
                Passkey,
                Password,
                SSO,
                Unknown2,
            )

        /** Parse a wire value, mapping anything unrecognized to [Unknown]. */
        public fun fromRawValue(rawValue: String): UserSessionsAuthMethod =
            when (rawValue) {
                "cross_app_auth" -> CrossAppAuth
                "external_auth" -> ExternalAuth
                "impersonation" -> Impersonation
                "magic_code" -> MagicCode
                "migrated_session" -> MigratedSession
                "oauth" -> OAuth
                "passkey" -> Passkey
                "password" -> Password
                "sso" -> SSO
                "unknown" -> Unknown2
                else -> Unknown(rawValue)
            }
    }

    internal object Serializer : KSerializer<UserSessionsAuthMethod> {
        override val descriptor: SerialDescriptor =
            PrimitiveSerialDescriptor("UserSessionsAuthMethod", PrimitiveKind.STRING)

        override fun deserialize(decoder: Decoder): UserSessionsAuthMethod = fromRawValue(decoder.decodeString())

        override fun serialize(
            encoder: Encoder,
            value: UserSessionsAuthMethod,
        ) {
            encoder.encodeString(value.rawValue)
        }
    }
}
